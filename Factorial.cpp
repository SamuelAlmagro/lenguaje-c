#include <stdio.h>
#include <stlib.h>
float arriba1 (float x, int i){
  return (1)^(i);
  }

int main (){
float x,resultado;
int n;
  printf("escribe el valor de x y de n");
  scanf("%f,%i",&x,&n);
  resultado=seno(angulo,n);
  prinf("resultado del seno es: %f",resulatado);
  }
